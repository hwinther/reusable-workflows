name: "Publish backend container to GHCR"

on:
  workflow_call:
    inputs:
      dotnet_version:
        description: ".NET version, e.g. 10.0.x"
        required: true
        type: string
      solution_path:
        description: "Solution path, e.g. backend"
        required: true
        type: string
      solution_file:
        description: "Solution file, e.g. ProjectName.slnx"
        required: true
        type: string
      project_file:
        description: "Project file, e.g. src/Api/Api.csproj"
        required: true
        type: string
      cache_enabled:
        description: "Enable NuGet package caching, requires dotnet lockfiles to be in use"
        required: false
        type: boolean
        default: true
      container_registry:
        description: "Container registry, e.g. ghcr.io"
        required: false
        type: string
        default: "ghcr.io"
      container_image_name_postfix:
        description: "Container image name postfix, e.g. api"
        required: true
        type: string
      override_config:
        description: "Override config"
        required: false
        type: string
        default: ""
      grype_scan_enabled:
        description: "Run Anchore Grype vulnerability scan before push (SBOM is still generated)"
        required: false
        type: boolean
        default: true
      grype_severity_cutoff:
        description: "Grype severity-cutoff (fail on this level and above): negligible, low, medium, high, critical"
        required: false
        type: string
        default: high
      grype_only_fixed:
        description: "Grype only-fixed: only report (and fail on) vulnerabilities that have a fix available"
        required: false
        type: boolean
        default: true
      grype_fail_build:
        description: "Grype fail-build: fail the job when vulnerabilities meet severity-cutoff (false = report/SARIF only)"
        required: false
        type: boolean
        default: true
      code_scanning_upload_enabled:
        description: "Upload the Grype SARIF to GitHub Code Scanning. Set to false on private repositories without GitHub Advanced Security; findings still go to the job summary, the PR comment, and the severity gate."
        required: false
        type: boolean
        default: true
      release_asset_upload_enabled:
        description: "Attach generated SBOMs to a matching GitHub Release as release assets (anchore/sbom-action default on tag pushes). Requires `contents: write` — set to false on private repos / workflows where the build job only has `contents: read`. SBOMs are still uploaded as workflow artifacts when false; only the release-asset attachment is skipped."
        required: false
        type: boolean
        default: true
      cosign_sign_enabled:
        description: "Sign the pushed image digest with cosign (keyless / GitHub OIDC); signatures are stored in the registry. Installs Cosign CLI v2.4.3 (cosign-installer cosign-release) so attestations use legacy sha256-….att registry manifests compatible with Kyverno 1.17 verifyImages; Cosign v3+ prefers OCI referrers, which that fetch path does not resolve."
        required: false
        type: boolean
        default: true
      cosign_supply_chain_attest_enabled:
        description: |
          Controls two things together: (1) generation of the CycloneDX SBOM and Grype
          JSON predicate files inside `_scan-image`, which are also uploaded as workflow
          artifacts (`container-image-${postfix}[-${suffix}].{cyclonedx,grype}.json`);
          (2) when `cosign_sign_enabled` is ALSO true, publishing those predicates as
          cosign CycloneDX + vuln attestations from this workflow (Kyverno-friendly).
          Decoupled from `cosign_sign_enabled` since v1.54.0: set this true with
          `cosign_sign_enabled: false` to generate and upload the predicates for a
          downstream consumer-side `sign-image` job (narrower OIDC trust scope) without
          publishing duplicate in-job cosign attestations. Set both false to skip both.
          Requires job permissions id-token:write and attestations:write when also signing.
          Relies on the Cosign v2.x pin so attestations publish as .att manifests, not
          referrers-only (Cosign v3+).
        required: false
        type: boolean
        default: true
      attest_build_provenance_enabled:
        description: "Generate a GitHub artifact attestation (actions/attest-build-provenance). Set to false on private repositories without GitHub Advanced Security / paid plan; Cosign-based signatures and attestations still run."
        required: false
        type: boolean
        default: true
      artifact_name_suffix:
        description: "Optional suffix appended to scan-related artifact names (SPDX/CycloneDX SBOM workflow artifacts, SARIF category, PR-comment identifier). Use when calling this workflow from a build matrix — pass the matrix dimension value (for example the matrix.debian_release expression evaluated at the call site) so each matrix entry uploads under a unique artifact name. Without it, matrix entries that share `container_image_name_postfix` collide with HTTP 409 on `actions/upload-artifact`."
        required: false
        type: string
        default: ""
      tag_suffix:
        description: |
          Optional suffix appended to every gitversion-derived image tag. With gitversion producing e.g. `:1.2.3 :1.2 :1 :main`, setting `tag_suffix: api` makes them `:1.2.3-api :1.2-api :1-api :main-api`. Use for matrix callers that want per-entry tagging without races on the shared gitversion tags.
        required: false
        type: string
        default: ""
      primary_nuget_source:
        description: "Primary NuGet source URL passed to actions/setup-dotnet via source-url."
        required: false
        type: string
        default: ""
      extra_nuget_source:
        description: "Additional private NuGet feed URL added via `dotnet nuget add source` before restore."
        required: false
        type: string
        default: ""
      extra_nuget_source_name:
        description: "Name for the extra NuGet source."
        required: false
        type: string
        default: ""
      extra_nuget_source_username:
        description: "Username for `dotnet nuget add source` on the extra source."
        required: false
        type: string
        default: "az"
    secrets:
      nuget_auth_token:
        description: "Auth token for NuGet feeds (NUGET_AUTH_TOKEN for setup-dotnet primary source and --password for the extra source). When unset, falls back to GITHUB_TOKEN."
        required: false
    outputs:
      container_image_tags:
        description: "Full image refs pushed (registry/name:tag, comma-separated). Reflects any tag_suffix applied during composition."
        value: ${{ jobs['build-and-push'].outputs.container_tags }}
      image_digest:
        description: "OCI manifest digest for the pushed image (sha256:...)"
        value: ${{ jobs['build-and-push'].outputs.image_digest }}

jobs:
  prepare-repository-name:
    runs-on: ubuntu-latest
    permissions: {}
    outputs:
      repository: ${{ steps.lower-repo.outputs.repository }}
    steps:
      - id: lower-repo
        run: |
          echo "repository=${GITHUB_REPOSITORY@L}" >> $GITHUB_OUTPUT

  gitversion:
    needs: prepare-repository-name
    permissions:
      contents: read
    uses: hwinther/reusable-workflows/.github/workflows/gitversion.yml@v1
    with:
      container_registry: ${{ inputs.container_registry }}
      container_image_name: ${{ needs.prepare-repository-name.outputs.repository }}/${{ inputs.container_image_name_postfix }}
      override_config: ${{ inputs.override_config }}

  build-and-push:
    name: Build and push (${{ needs.gitversion.outputs.container_image_tags }})
    needs: [prepare-repository-name, gitversion]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
      actions: write
      security-events: write
      pull-requests: write  # for the Grype findings PR comment via _grype-summary
    outputs:
      image_digest: ${{ steps.push.outputs.image_digest }}
      container_tags: ${{ steps.tags.outputs.container_tags }}
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@650006c6eb7dba73a995cc03b0b2d7f5ca915bee # v4.2.0
        with:
          registry: ${{ inputs.container_registry }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build dotnet container (publish to archive, load locally)
        id: build
        uses: hwinther/reusable-workflows/.github/actions/_build-image-dotnet@v1
        with:
          dotnet_version: ${{ inputs.dotnet_version }}
          solution_path: ${{ inputs.solution_path }}
          solution_file: ${{ inputs.solution_file }}
          project_file: ${{ inputs.project_file }}
          cache_enabled: ${{ inputs.cache_enabled }}
          image_tags: ${{ needs.gitversion.outputs.image_tags }}
          repository: ${{ needs.prepare-repository-name.outputs.repository }}
          container_image_name_postfix: ${{ inputs.container_image_name_postfix }}
          version: ${{ needs.gitversion.outputs.version }}
          git_sha: ${{ github.sha }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          nuget_auth_token: ${{ secrets.nuget_auth_token }}
          primary_nuget_source: ${{ inputs.primary_nuget_source }}
          extra_nuget_source: ${{ inputs.extra_nuget_source }}
          extra_nuget_source_name: ${{ inputs.extra_nuget_source_name }}
          extra_nuget_source_username: ${{ inputs.extra_nuget_source_username }}

      - name: Generate SBOM and scan with Grype
        id: scan
        uses: hwinther/reusable-workflows/.github/actions/_scan-image@v1
        with:
          image_ref: ${{ steps.build.outputs.local_ref }}
          container_image_name_postfix: ${{ inputs.container_image_name_postfix }}
          artifact_name_suffix: ${{ inputs.artifact_name_suffix }}
          grype_scan_enabled: ${{ inputs.grype_scan_enabled }}
          grype_severity_cutoff: ${{ inputs.grype_severity_cutoff }}
          grype_only_fixed: ${{ inputs.grype_only_fixed }}
          grype_fail_build: ${{ inputs.grype_fail_build }}
          code_scanning_upload_enabled: ${{ inputs.code_scanning_upload_enabled }}
          release_asset_upload_enabled: ${{ inputs.release_asset_upload_enabled }}
          # Decoupled from cosign_sign_enabled in v1.54.0 so consumer-signed callers can
          # generate + upload the SBOM/Grype predicates without publishing duplicate in-job
          # cosign attestations. The cosign attest steps inside _push-image-with-signatures
          # remain independently gated on cosign_sign_enabled, so we still won't attest from
          # this workflow's identity when signing is off.
          cosign_supply_chain_attest_enabled: ${{ inputs.cosign_supply_chain_attest_enabled }}
          github_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Compose final container tags
        id: tags
        shell: bash
        env:
          GITVERSION_TAGS: ${{ needs.gitversion.outputs.container_image_tags }}
          TAG_SUFFIX: ${{ inputs.tag_suffix }}
        run: |
          set -euo pipefail
          if [ -n "$TAG_SUFFIX" ]; then
            # Each gitversion entry is "registry/repo/postfix:tag" — append "-${TAG_SUFFIX}" to ":tag".
            final_tags=""
            while IFS= read -r ref; do
              ref=$(echo "$ref" | xargs)
              [ -z "$ref" ] && continue
              suffixed="${ref}-${TAG_SUFFIX}"
              if [ -z "$final_tags" ]; then
                final_tags="$suffixed"
              else
                final_tags="${final_tags},${suffixed}"
              fi
            done < <(echo "$GITVERSION_TAGS" | tr ',' '\n')
          else
            final_tags="$GITVERSION_TAGS"
          fi
          echo "container_tags=$final_tags" >> "$GITHUB_OUTPUT"

      - name: Push image and attach signatures + attestations
        id: push
        uses: hwinther/reusable-workflows/.github/actions/_push-image-with-signatures@v1
        with:
          image_ref: ${{ steps.build.outputs.local_ref }}
          container_tags: ${{ steps.tags.outputs.container_tags }}
          container_registry: ${{ inputs.container_registry }}
          repository: ${{ needs.prepare-repository-name.outputs.repository }}
          container_image_name_postfix: ${{ inputs.container_image_name_postfix }}
          cosign_sign_enabled: ${{ inputs.cosign_sign_enabled }}
          cosign_supply_chain_attest_enabled: ${{ inputs.cosign_supply_chain_attest_enabled }}
          attest_build_provenance_enabled: ${{ inputs.attest_build_provenance_enabled }}
          sbom_cyclonedx_path: ${{ steps.scan.outputs.sbom_cyclonedx_path }}
          grype_json_path: ${{ steps.scan.outputs.grype_json_path }}
          version: ${{ needs.gitversion.outputs.version }}

      - name: Job summary
        env:
          CONTAINER_TAGS: ${{ needs.gitversion.outputs.container_image_tags }}
          IMAGE_REF: ${{ inputs.container_registry }}/${{ needs.prepare-repository-name.outputs.repository }}/${{ inputs.container_image_name_postfix }}@${{ steps.push.outputs.image_digest }}
          COSIGN_CERT_IDENTITY: https://github.com/${{ github.workflow_ref }}
          WORKFLOW_REF: ${{ github.workflow_ref }}
          IMAGE_DIGEST: ${{ steps.push.outputs.image_digest }}
          APP_VERSION: ${{ needs.gitversion.outputs.version }}
          GIT_SHA: ${{ github.sha }}
          LOCAL_REF: ${{ steps.build.outputs.local_ref }}
          COSIGN_SUPPLY_CHAIN_ATTEST: ${{ inputs.cosign_sign_enabled && inputs.cosign_supply_chain_attest_enabled }}
        run: |
          echo "### 📦 Container image" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "The following tags were pushed to the registry:" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "$CONTAINER_TAGS" | tr ',' '\n' | while read -r tag; do
            tag=$(echo "$tag" | xargs)
            [ -z "$tag" ] && continue
            echo "- [\`${tag}\`](https://${tag})" >> $GITHUB_STEP_SUMMARY
          done
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Image digest: \`${IMAGE_DIGEST}\`" >> $GITHUB_STEP_SUMMARY
          IMAGE_SIZE_BYTES=$(docker image inspect --format='{{.Size}}' "$LOCAL_REF" 2>/dev/null || true)
          if [ -n "${IMAGE_SIZE_BYTES:-}" ]; then
            IMAGE_SIZE_HR=$(numfmt --to=iec --suffix=B --format="%.2f" "$IMAGE_SIZE_BYTES")
            echo "Image size (uncompressed): \`${IMAGE_SIZE_HR}\` (${IMAGE_SIZE_BYTES} bytes)" >> $GITHUB_STEP_SUMMARY
          fi
          echo "Publish properties: \`ContainerVersion\` / \`InformationalVersion\` = \`${APP_VERSION}\`, \`SourceRevisionId\` = \`${GIT_SHA}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "<details>" >> "$GITHUB_STEP_SUMMARY"
          echo "<summary><strong>Verify digest, signature, and attestation</strong></summary>" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "GHCR may list several manifests in one package (image, cosign \`sha256-…\` signature tags, build attestations). They refer to the **same** image identity via digest:" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "- Immutable reference: \`${IMAGE_REF}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "**cosign** (keyless: the certificate identifies **this reusable workflow**, not the consumer repo. Pin consumers with \`uses: …/dotnet-container.yml@v1\` so verification can target \`@refs/tags/v1\`.):" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "- This run: \`${WORKFLOW_REF}\` → identity \`${COSIGN_CERT_IDENTITY}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "Verify **this exact run** (authenticate to GHCR if prompted):" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "\`\`\`shell" >> "$GITHUB_STEP_SUMMARY"
          echo "cosign verify --certificate-oidc-issuer \"https://token.actions.githubusercontent.com\" --certificate-identity \"${COSIGN_CERT_IDENTITY}\" \"${IMAGE_REF}\"" >> "$GITHUB_STEP_SUMMARY"
          echo "\`\`\`" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "Policy-style verify when consumers use the floating **v1** tag (any org/repo hosting this workflow):" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "\`\`\`shell" >> "$GITHUB_STEP_SUMMARY"
          echo "cosign verify --certificate-oidc-issuer \"https://token.actions.githubusercontent.com\" --certificate-identity-regexp '^https://github.com/[^/]+/[^/]+/\\.github/workflows/dotnet-container\\.yml@refs/tags/v1\$' \"${IMAGE_REF}\"" >> "$GITHUB_STEP_SUMMARY"
          echo "\`\`\`" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "If verification fails, add \`--verbose\` and align \`--certificate-identity\` / \`-regexp\` with the \`Issued certificate\` line." >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          if [ "${COSIGN_SUPPLY_CHAIN_ATTEST}" = "true" ]; then
            echo "**Cosign supply-chain attestations** (CycloneDX SBOM + Grype vuln, e.g. for Kyverno \`verifyImages\` / \`validate\`):" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "List signature and attestation layers (authenticate to GHCR if prompted):" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "\`\`\`shell" >> "$GITHUB_STEP_SUMMARY"
            echo "cosign tree \"${IMAGE_REF}\"" >> "$GITHUB_STEP_SUMMARY"
            echo "\`\`\`" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "Verify attestations with the **same** certificate identity as \`cosign verify\` above:" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "\`\`\`shell" >> "$GITHUB_STEP_SUMMARY"
            echo "cosign verify-attestation --certificate-oidc-issuer \"https://token.actions.githubusercontent.com\" --certificate-identity \"${COSIGN_CERT_IDENTITY}\" --type cyclonedx \"${IMAGE_REF}\"" >> "$GITHUB_STEP_SUMMARY"
            echo "cosign verify-attestation --certificate-oidc-issuer \"https://token.actions.githubusercontent.com\" --certificate-identity \"${COSIGN_CERT_IDENTITY}\" --type vuln \"${IMAGE_REF}\"" >> "$GITHUB_STEP_SUMMARY"
            echo "\`\`\`" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "With a floating **v1** workflow pin, use \`--certificate-identity-regexp\` the same way as for \`cosign verify\` (see \`.github/workflows/dotnet-container.yml\` job summary source for the pattern)." >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
          fi
          echo "**GitHub artifact attestation** (subject is this digest):" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "\`\`\`shell" >> "$GITHUB_STEP_SUMMARY"
          echo "gh attestation verify oci://${IMAGE_REF} --repo ${GITHUB_REPOSITORY}" >> "$GITHUB_STEP_SUMMARY"
          echo "\`\`\`" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "If \`gh\` errors with \`PKIX_ED25519\` / trusted root / Sigstore, upgrade the GitHub CLI to the latest release (older \`gh\` builds cannot load the current Sigstore trust bundle)." >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "</details>" >> "$GITHUB_STEP_SUMMARY"
